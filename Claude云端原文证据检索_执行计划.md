# Claude 云端原文证据检索执行计划

## 最高目标

在 Claude 云端包尽量小、依赖尽量少的前提下，让中文搜英文、盘面事实匹配、前 50 候选召回、父段回取和出处完整度尽量接近本地满血检索器。

最终模式已经确定：

> 我们本地制作 BGE-M3 的 ONNX int8 查询编码器、卡片向量和只读索引；Claude 云端只编码用户当前的问题、查询现成索引、阅读候选并选出证据。Claude 不重新编码全书，不自己永久加书，也不建立第二套书库。

这里不训练新模型、不微调占星知识。只是把现有 BGE-M3 转成适合两核 CPU 使用的量化查询编码器。

交付优先级已经调整：先交付可以常驻 Skill 的数据包，再单独测试模型包能否进入 Skill。不能因为模型上传受限而卡住数据、契约和查询脚本的交付。

## 一、人话总纲

三部分各做一件事：

1. 原文证据库负责“书上到底写了什么”，必须带书名、版本、章、偈和 PDF 页码。
2. BGE-M3 查询编码器负责“先从上万张卡里捞出可能相关的前 50 张”，它只是搜索工具，不负责判断吉凶。
3. Claude 负责“听懂用户问题、判断候选卡条件是否真的匹配、组织成人话”，不能用训练记忆冒充书上原文。

一句话：证据靠库，粗捞靠查询编码器，判断靠 Claude。

## 二、本地和 Claude 云端怎样分工

| 工作 | 我们本地 | Claude 云端 |
|---|---|---|
| 验收新书成品 ZIP | 负责 | 不负责 |
| 保存唯一原始卡片 | 负责 | 只接收发布副本 |
| 建立 SQLite 全文索引 | 负责 | 只读查询 |
| 生成卡片向量 | 负责 | 不重新计算全书 |
| 制作量化查询编码器 | 负责 | 只负责运行 |
| 规范词表和结构字段 | 负责 | 原样使用 |
| 新书替换、回滚和版本管理 | 负责 | 接收新版查询包 |
| 理解中文问题 | 不负责 | 负责 |
| 将问题拆成梵文、英文和盘面事实 | 不负责 | 负责 |
| 从前 50 张候选里判断条件是否匹配 | 不负责 | 负责 |
| 回取完整父段和展示出处 | 提供程序和数据 | 调用并展示 |

任何时候都不能把“Claude 能理解问题”写成“Claude 可以不用证据库”。Claude 的训练知识只能帮助理解，不能代替可回查的书页证据。

## 三、Claude 云端实际环境

以下以 Claude 侧最新实测为准，替代此前“PyPI 完全不可用”的旧描述：

| 项目 | 实际环境 |
|---|---|
| 系统 | Linux，Debian 系 |
| Python | 3.11 |
| CPU | 2 核，无 GPU |
| 内存 | 7 GB，总可用约 6 GB |
| 磁盘 | 可用约 22 GB |
| 已装 | onnxruntime 1.25.0、tokenizers 0.22.2、transformers 5.14.1、numpy、sqlite3 3.45.1、bm25s、lancedb 0.36.0 |
| SQLite | FTS5 全文搜索已实测可用 |
| 网络 | PyPI 可达；HuggingFace、镜像站、ModelScope 和 GitHub 不可达 |
| Skill 目录 | 只读；查询时不得写入模型目录或索引目录 |

模型不能在 Claude 云端临时下载，必须由我们本地制作后提供；它可以作为独立 Skill 常驻，也可以在常驻失败时由用户上传到当前会话。当前虽然已装 LanceDB 和 bm25s，但本方案不使用它们；SQLite FTS5 已经够用，不额外增加数据库。

模型文件必须兼容这套已实测环境：Linux Debian 系、x86_64、Python 3.11、onnxruntime 1.25.0 的 `CPUExecutionProvider（只用 CPU 的运行方式）`。模型包不再携带 PyTorch、CUDA 或另一份 ONNX Runtime。

## 四、完整查询链路

```text
用户原始中文或英文问题
  ↓
Claude 拆成：英文检索表达＋梵英双写实体＋用户明确给出的盘面事实
  ↓
BGE-M3 查询编码器为当前问题生成一个 1024 维向量
  ↓
结构匹配＋SQLite 全文搜索＋向量相似搜索
  ↓
按现有简单排名融合规则合并，得到前 50 张候选短卡
  ↓
Claude 阅读候选，判断原文条件是否真的匹配当前问题
  ↓
选出主结果前 8 张，并从其他命中书中选补充证据
  ↓
按 parent_id 回取完整父段
  ↓
显示原文、书名、版本、章、偈、PDF 页码和父段编号
```

Claude 不能直接把原始中文句子丢给英文关键词引擎。真实链路必须先经过 Claude 拆解，再进入检索器。

## 五、Claude 怎样拆问题

### 1. 拆解原则

- 每个明确实体都使用规范词表展开成梵文名和英文名，例如 `Shani OR Saturn`、`Chandra OR Moon`。
- 用户明确提供的星体、宫位、宫主、星座、相位和状态，必须进入结构事实。
- 用户没有明确给出的盘面事实不能伪装成已知事实。
- 为同一个主题增加英文同义表达可以用于扩大搜索，但这些表达只能作为搜索词，不能直接写进结论。
- 查询脚本接收结构化 JSON，不直接执行 Claude 手写的 FTS 布尔字符串，避免括号、加号和特殊字符导致查询错误。

### 2. 查询计划格式

```json
{
  "original_query": "母亲身体不好在盘上怎么看",
  "search_phrases": [
    "mother health afflicted",
    "evils to mother"
  ],
  "entity_ids": [
    "HOUSE_4",
    "MOON"
  ],
  "explicit_facts": [],
  "filters": {
    "work_ids": []
  }
}
```

`explicit_facts（明确盘面事实）`只能放用户真实说出的配置。例如用户说“土星相位月亮”，可以写入土星、相位、月亮三项关系；“母亲健康不好怎么看”没有给出实际星盘配置，只能放主题词和相关实体，不能写成“月亮已经受克”。

### 3. 已确认的拆解样本

| 原始问题 | Claude 实际用于搜索的表达 |
|---|---|
| 母亲身体不好在盘上怎么看 | `(Matru OR mother OR 4th) AND (Chandra OR Moon)`，配合 `afflicted`、`evils to mother` |
| 财运好但留不住钱 | `(Dhana OR 2nd lord) AND (Vyaya OR 12th OR loss of wealth)`；另查 `(Labha OR 11th lord) AND (Randhra OR 8th)`，只作为搜索假设，不作为用户盘面事实 |
| 处女座上升性格 | `(Kanya OR Virgo) AND (Lagna OR ascendant)`，配合 `characteristics`、`disposition` |
| 金星双鱼入旺 | `(Shukra OR Venus) AND (Meena OR Pisces) AND (exalt OR uchcha)` |
| 月亮被焚且弱 | `(Chandra OR Moon) AND (combust OR Asta OR weak OR debilitat)` |
| 看结婚时间 | `time of marriage`，配合 `(Yuvati OR 7th lord)` 和 `(Dasha OR Bhukti)` |

其余固定题按同一规则生成。测试报告必须同时保存原始问题和 Claude 拆解后的查询计划，不能只保存其中一份。

## 六、规范词表

`canonical_vocab.json（规范词表）`是全系统共享资产，至少覆盖：

- 九曜。
- 十二宫。
- 十二星座。
- 宫主关系。
- 相位关系。
- 尊贵、落陷、逆行、灼伤等状态。
- 已确认的梵文、英文、中文表层写法和常见拼写变体。

规范词表只收录明确同义词和已确认变体，禁止为了提高命中率随意扩展占星含义。查询计划中的实体必须先归到规范实体编号，再用于结构匹配。

## 七、量化查询编码器规格

### 1. 模型

| 项目 | 规格 |
|---|---|
| 基座 | BAAI/bge-m3，与本地检索器同源 |
| 运行格式 | ONNX |
| 量化 | int8 动态量化，面向两核 CPU |
| 输出 | 只保留 dense 1024 维向量 |
| 不需要 | sparse 分支、ColBERT 分支、精排模型 |

不使用 fp16 模型在 CPU 上推理。fp16 只用于保存已经生成好的卡片向量，减少包体积。

### 2. 必须随包提供的模型文件

- `model_quantized.onnx`
- `config.json`
- `tokenizer.json`
- `tokenizer_config.json`
- `special_tokens_map.json`
- `sentencepiece.bpe.model`

实际模型仓库缺少某个非必需文件时，以真实分词器能否加载为准，不为了凑清单制作假文件。

这些文件单独组成模型 ZIP，不与第一批数据包捆在一起。先试传一次模型 ZIP，验证 Claude Skill 的实际上传和持久化能力；“目前最大已有 Skill 只有 8.8 MB”只能说明风险很高，不能当作正式上传上限。

### 3. 向量生成必须完全一致

卡片向量和查询向量必须使用：

- 同一个 ONNX int8 模型文件。
- 同一套 tokenizer（把文字切成模型可读小块的文件）。
- 同一最大长度和截断规则。
- 同一 dense 输出位置。
- 同一池化方式。
- 同一 L2 归一化方式。

不能使用 fp32 模型生成卡片向量、再使用 int8 模型生成查询向量。否则虽然都叫 BGE-M3，实际数字坐标仍可能发生偏移。

这不是重新训练小模型，只是把现有 BGE-M3 转换并量化。

## 八、卡片向量和索引怎样制作

所有卡片向量都在我们本地生成，不在 Claude 云端批量编码。

固定流程：

1. 本地验收新书成品 ZIP。
2. 使用正式 ONNX int8 查询编码器生成该书全部可检索卡片的向量。
3. 向量先做 L2 归一化，再以 fp16 保存为 `embeddings.npy`。
4. SQLite 每张卡记录 `embedding_row（该卡在向量文件中的行号）`，不再另建一份容易漂移的卡片顺序表。
5. Claude 云端读取 `embeddings.npy`，只为当前查询生成一个向量，再通过 NumPy 点积寻找相近卡片。

卡片原文、完整父段、citation、结构实体和 `embedding_row` 全部保存在 SQLite。根据当前交付要求，数据 Skill 同时保留一份人可读的 `source_cards.jsonl`；SQLite 和 JSONL 必须由同一次本地构建生成，Claude 云端不得分别修改。原始书包仍由本地永久保存。

## 九、三路粗捞和前 50 候选

每次查询使用三路候选：

1. 结构匹配：处理星体、宫位、宫主、落座、相位和状态等明确盘面事实。
2. SQLite FTS5 全文搜索：处理梵文、英文术语和明确关键词。
3. BGE-M3 向量搜索：处理中英文措辞不同但意思接近的内容。

三路结果按现有简单的倒数排名融合合并；倒数排名融合就是按每一路的名次加分，不比较三种完全不同的原始分数。最终保留前 50 张候选供 Claude 阅读。

标签和元数据只能加分，默认不做硬过滤。用户明确要求“只查某本书”时才使用 `work_id（作品编号）`限制范围。

只允许两种去重：

- 同一卡片编号重复命中时合并。
- 同一个父段的多个子片命中时合并，最终回取一次完整父段。

不同书、不同章、不同页的相近原文必须分别保留，禁止跨出处合并。

## 十、Claude 怎样筛选候选

Claude 阅读前 50 张短卡后，重点判断：

- 卡片的触发条件是否与用户盘面事实一致。
- 卡片讨论的是不是用户当前问题。
- 卡片是否只是概念相似，但条件实际对不上。
- 多本书是否提供同向、相反或互相修正的证据。

不携带 bge-reranker（本地精排模型）。原因不是精排模型没有价值，而是 Claude 需要判断“原文条件是否匹配当前盘面”，这比单纯判断两段文字像不像更重要。

主结果返回前 8 个完整父段。前 50 候选中，未进入主结果但有命中的其他书，每本取最佳 1 条放入 `cross_book_supplement（其他书补充结果）`，不占主结果位置。

## 十一、not_found 规则

本地精排模型使用的中文 0.03、英文 0.10 分数不能搬到云端，因为这里已经不是同一种分数。

`not_found（本次未搜到）`必须非常保守：

- 只有 Claude 阅读候选后确认没有一条实际回答当前问题时才能返回。
- 不确定时宁可返回较弱候选并明确说明“相关性较弱”，不能假装库里没有。
- `not_found`只表示本次没有搜到，不等于原书绝对没有。
- 固定题测试中的错误 `not_found` 必须为 0，这是最高优先级验收项。

每次查询必须明确显示当前运行状态：

- `full_hybrid（完整检索）`：结构匹配、全文搜索和 BGE-M3 向量搜索三路都已运行。
- `lexical_only（临时检索）`：当前会话没有加载模型，只运行结构匹配和全文搜索。

`lexical_only（临时检索）`不能冒充本地满血效果，也不能覆盖完整检索的固定 20 题基线。

## 十二、两个交付包

### 1. 先交付：常驻数据 Skill 包

```text
claude-vedic-evidence-retrieval/
  SKILL.md
  README.md
  TUTORIAL.md
  scripts/
    evidence_cli.py
  config/
    canonical_vocab.json
    books.json
    questions_20.json
  data/
    source_cards.jsonl
    evidence.sqlite
    embeddings.npy
    index_manifest.json
  reference/
    PACKAGE_CONTRACT.md
    retrieval_rules.md
```

这个包优先制作和交付，目标是先把卡片、预计算向量、规范词表、契约、查询脚本和说明常驻到 Claude Skill。`source_cards.jsonl`保留为人可读原始卡副本，SQLite 是查询用派生索引；两者都由同一次本地打包生成，禁止在 Claude 云端分别修改。

第一批数据包即使暂时没有模型，也必须能运行结构匹配、SQLite 全文搜索、父段回取和 citation 展示，并明确标记为 `lexical_only（临时检索）`。

### 2. 后交付：单独模型包

```text
claude-bge-m3-query-model/
  SKILL.md
  MODEL_README.md
  model_quantized.onnx
  config.json
  tokenizer.json
  tokenizer_config.json
  special_tokens_map.json
  sentencepiece.bpe.model
  model_manifest.json
```

模型包只包含量化查询编码器和分词文件，不包含卡片、向量、SQLite、精排模型或运行环境。

先把模型 ZIP 单独上传为 Skill 做一次真实测试：

1. 上传成功并能跨会话调用：模型作为第二个常驻 Skill 保存，数据 Skill 通过明确模型目录调用。
2. 上传失败或无法跨会话保持：模型 ZIP 改为单次会话文件，需要完整向量检索时由 Claude 在当前会话上传并解压到可写工作目录。

无论模型常驻还是单次会话上传，数据 Skill 都保持不变。查询命令通过显式 `--model-dir` 接收当前模型目录，不自动扫描硬盘，也不把临时路径写回 Skill。

### 3. 单次会话模型的使用方式

```text
常驻数据 Skill
  ＋
当前会话上传的模型 ZIP
  ↓
解压到当前工作目录或临时目录
  ↓
evidence_cli.py --model-dir <本次模型目录>
  ↓
full_hybrid（完整检索）
```

新会话如果没有上传模型，只能运行 `lexical_only（临时检索）`；不能假装向量检索已经执行。

不进入云端包：

- bge-reranker 精排模型。
- PyTorch 和 CUDA 环境。
- LanceDB 索引。
- 原 PDF。
- 图片资产。
- OCR 中间文件。
- 本地虚拟环境。
- 由 Claude 云端重新生成的第二份卡片或向量。

数据包和模型包体积都以本地真实打包结果为准。“数据包几十 MB、模型包约 600 MB”只作为当前估算，不写成保证值。

## 十三、库存、版本和书籍范围

`index_manifest.json（索引清单）`是云端包版本和数量的唯一事实源，至少记录：

- `index_version（索引版本）`。
- 成品包契约版本。
- ONNX 模型身份和量化版本。
- tokenizer、截断、池化和归一化口径。
- 当前书目和永久 `work_id`。
- 每本书原始卡数、参与搜索卡数和父段数。
- 全库对应总数。
- 本版新增、替换或移除了哪些书。
- `source_cards.jsonl`、SQLite 和 `embeddings.npy`是否来自同一次构建。

SKILL.md 顶部显示“本版新增书、当前书数和卡数”，但这段内容必须在本地打包时从 `index_manifest.json` 自动生成，不能手工维护第二份数字。

`books.json`可以记录每本书主要覆盖的技法范围，帮助 Claude 选择查询范围和解释库存。但它不能单独证明“原书没有相关说法”，也不能默认把未标记的书排除出搜索。

## 十四、新书加入与版本替换

新书固定在我们本地处理：

1. 切书窗口交付符合 `PACKAGE_CONTRACT.md` 的成品 ZIP。
2. 本地运行验包；字段缺失、父子关系断裂或 citation 不完整时直接报错，不猜测补值。
3. 更新本地满血索引并运行固定 20 题回归。
4. 使用正式 ONNX int8 模型为新书生成云端卡片向量。
5. 增量更新 Claude 的 SQLite 和 `embeddings.npy`。
6. 运行云端模拟测试和固定 20 题。
7. 通过后生成新版 Claude Skill ZIP，交给 Claude 上传替换。

同一本书出现修正版时保持永久 `work_id`不变，整本替换旧版本。不得让新旧卡混在同一正式索引中。

Claude 云端不能接收原始新书后自行永久建库。它只接收我们已经验收、编码、回归并打包好的查询版。

## 十五、本地模拟 Claude 环境

本地测试时强制使用 ONNX Runtime 的 CPU 模式和两核线程：

```python
import onnxruntime as ort

options = ort.SessionOptions()
options.intra_op_num_threads = 2
options.inter_op_num_threads = 1

session = ort.InferenceSession(
    "model_quantized.onnx",
    options,
    providers=["CPUExecutionProvider"],
)
```

测试必须使用 CPU，不得偷偷调用本机 3080。内存占用以进程实测峰值为准，要求低于 Claude 约 6 GB 的可用范围。

## 十六、固定 20 题测试口径

### 1. 不把原始中文直接交给关键词引擎

真实测试链路是：

```text
原始中文题
  ↓
Claude 生成查询计划
  ↓
检索器执行查询计划
```

本地检索引擎测试使用已经冻结的 Claude 查询计划；Claude 云端端到端测试再从原始中文开始。两份报告都保存原始问题和拆解结果。

### 2. 核心指标

| 指标 | 通过要求 |
|---|---|
| 单条查询编码耗时 | 两核 CPU 下小于 2 秒 |
| 前 50 候选召回 | 本地满血版已经确认的好证据进入前 50即通过 |
| 错误 not_found | 必须为 0 |
| int8 与 fp32 向量差异 | 抽样余弦相似度大于 0.99 |
| citation | 书名、版本、章、偈号状态、PDF 页码和父段编号完整 |
| 父段回取 | 同一父段只返回一次，正文完整 |

前 8 的顺序不作为查询编码器是否合格的主要指标，因为最终排序由 Claude 阅读条件后完成。但最终前 8 仍需检查有没有明显不相关内容，以及多书证据是否被错误吞掉。

### 3. 当前阶段真实产物与结果

- 本地正式书架已完成八本：只保留 Sareen 版《果灯论》，另含 Uttara Kalamrita、星宿书和两版 BPHS；Kapoor 版不进入正式索引或 Claude 数据包。
- 本地索引共 `24668` 张原文卡，其中 `24409` 张可搜索；固定 20 题最终回归通过。
- 两版 BPHS 共用 `source_group=BPHS`，整本暂挂 `["判词","时间"]`；20 题中两版同时进入前 8 仅 `1/20 = 5%`，低于 20% 观察线，暂不增加同源候选合并。
- 本地第 20 题发现 50 张混合候选会漏掉一张精排 `0.993` 的《萨拉瓦利》直接判词；现已保留原 50 张混合候选，并补入语义前 75 张去重后精排，旧高分结果没有被低分结果挤掉。
- 八本 Claude 数据包：`J:\\RAG索引\\Claude云端交付\\20260810\\claude-vedic-evidence-data-latest.zip`，`62,831,076` bytes；包含 8 本、24668 张原文卡、24409 张可搜索卡和 23581 个父段。
- 本次复用旧四本 `8875` 张向量，新编码 `15534` 张；同模型重算 20 张余弦最小值/均值均为 `1.0`，自检索 `40/40 = 100%`。
- 云端真实查询复核：候选原样返回 `source_group`；两版 BPHS 均为 `source_group=BPHS`。第 20 题旧《萨拉瓦利》目标卡未进前 50，但第 9 名返回 Sareen《果灯论》“Moon occupies the 8th house”的直接判词，属于同等好证据，不算退步。
- 模型包 ZIP：`J:\RAG索引\Claude云端交付\20260809\claude-bge-m3-query-model-v1.zip`，`591,774,965` bytes。
- 同一 ONNX int8 文件逐张重算的 20 张样本余弦最小值和均值均为 `1.0`；此前 `0.982` 来自不同编码路径，已停止使用该口径。
- 真实 Claude 上传、Skill 常驻、单会话上传和跨会话保持仍未完成。

### 4. 失败定位

- 好证据没有进入前 50：检查 Claude 查询拆解、规范词表、结构匹配、向量生成和三路合并。
- 好证据进入前 50，但没有进入最终前 8：检查 Claude 的筛选说明，不改查询编码器。
- citation 错误：回到本地入库或成品包修复，不让 Claude 猜出处。
- 出现错误 `not_found`：最高优先级修复，不能靠降低证据标准掩盖。

## 十七、完成标准

- [x] BGE-M3 已转换为只输出 dense 1024 维的 ONNX int8 查询编码器。
- [x] 卡片和查询使用完全相同的模型、分词、截断、池化和归一化口径。
- [x] 所有卡片向量由本地生成，并以 fp16 写入 `embeddings.npy`。
- [x] SQLite 通过 `embedding_row`准确关联卡片和向量。
- [x] Claude 云端只编码当前问题，不重新编码全书。
- [x] 常驻数据 Skill 包可以在没有模型时运行，并明确标记 `lexical_only（临时检索）`。
- [x] 模型目录通过 `--model-dir`显式传入，不写死 Claude 会话路径。
- [ ] 模型 ZIP 已单独试传并记录能否常驻 Skill。
- [ ] 模型不能常驻时，单次会话上传和工作目录加载已经实测可用。
- [x] 中文问题先由 Claude 拆成梵英双写词和明确盘面事实。
- [x] 结构、全文和向量三路粗捞能产生前 50 候选。
- [ ] 固定 20 题的好证据能进入前 50。
- [ ] 固定 20 题错误 `not_found` 为 0。
- [x] 两核 CPU 单条查询编码小于 2 秒（实测 `0.08784` 秒，batch4）。
- [x] 卡片和查询用同一 ONNX int8 文件逐张重算，20 张样本余弦最小值和均值均为 `1.0`。
- [x] 父段回取、去重、跨书补充和 citation 正确。
- [x] 新书只在本地加入，Claude 只接收新版查询包。
- [x] 正式包内没有精排模型、PyTorch、LanceDB、PDF、图片和由 Claude 另行生成的第二份卡片数据。

## 十八、执行顺序

- [x] 模式二已经确认：云端携带查询编码器和预计算卡片向量。
- [x] 已确认不携带 bge-reranker 精排模型。
- [x] 已确认卡片在本地编码，Claude 只编码当前查询。
- [x] 已确认测试必须使用 Claude 拆解后的查询计划。
- [x] 已确认先交付数据 Skill 包，模型包单独测试上传。
- [x] 锁定现有 BGE-M3 模型文件和转换来源。
- [x] 导出 dense 1024 维 ONNX 模型并完成 int8 动态量化。
- [x] 用同一 int8 模型生成现有书库的云端卡片向量。
- [x] 生成 SQLite、`embedding_row`和 `index_manifest.json`。
- [x] 编写单一 `evidence_cli.py`和 Claude SKILL.md。
- [ ] 冻结固定 20 题的 Claude 查询计划。
- [x] 在本地模拟两核 CPU 环境完成验收。
- [x] 优先打包并交付常驻数据 Skill ZIP。
- [x] 单独打包模型 ZIP；能否常驻 Claude Skill 仍未测试。
- [ ] 若模型不能常驻，实测单次会话上传、解压和 `--model-dir`加载。
- [ ] 在真实 Claude 环境运行固定 20 题。
- [ ] 与本地满血版对照并保存第一版云端基线。

## 十九、最终纪律

- 只有一套原始卡片，由本地成品包负责。
- 只有本地可以永久加书、替换书和生成卡片向量。
- 数据 Skill 常驻，模型是否常驻由一次真实上传测试决定。
- 模型不能常驻时，单次会话路径必须显式传入，不在 Skill 中保存临时路径。
- Claude 的训练知识不能作为证据。
- 查询编码器只负责找候选，不能判断占星结论。
- Claude 只能从检索结果判断条件是否匹配，不能编造原文或出处。
- 不为方便而跨书合并相近原文。
- 不把“本次没搜到”写成“原书没有”。
- 找到第一个真实可用、与本地满血版接近的实现后停止，不增加第二模型或备用检索框架。
